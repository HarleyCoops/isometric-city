# Optional: Supabase configuration for multiplayer/co-op features
# If not set, the game will work in single-player mode only.
# To enable multiplayer, create a Supabase project and add these values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
